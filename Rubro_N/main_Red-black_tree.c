#include <stdio.h>
#include <stdlib.h>
#include "Red-Black_Tree.c"

int main(){
  Tree *root;
  insert_tree(&root, 76);
  insert_tree(&root,43);
  insert_tree(&root,87);
  insert_tree(&root,54);
  insert_tree(&root,89);


  return 0;
}
